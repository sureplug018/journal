// const EditorGroup = require('../models/editorGroupsModel');
// const handleResponse = require('../utilities/appResponse');

// exports.createEditorGroup = async (req, res) => {
//   const { name } = req.body;

//   if (!name) {
//     return handleResponse(res, 400, 'fail', 'Name is required');
//   }
//     try {
//       const newEditorGroup = await 
//   } catch (err) {
//     return handleResponse(res, 500, 'fail', err.message);
//   }
// };
